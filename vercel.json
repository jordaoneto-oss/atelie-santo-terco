{
  "framework": "vite",
  "buildCommand": "cp -r backend/src api/backend-src && cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm install && cd ../api && npm install",
  "rewrites": [
    { "source": "/_/backend/api/(.*)", "destination": "/api/index.js" },
    { "source": "/api/(.*)", "destination": "/api/index.js" }
  ]
}
