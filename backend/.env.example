# Para rodar localmente com SQLite (não precisa configurar nada)
# Apenas rode: npm install && npm run seed && npm run dev

# Para deploy no Vercel com PostgreSQL (Supabase):
# DATABASE_URL=postgresql://usuario:senha@host:5432/database
